
# ROUGH IDEA
# name Flow which is always stage 0
# should be very logically based and heavyly functional
# parameters: number of stages, rules (could be a class and recieves <CONTEXT>),
# generator which should be called within a while True
# keys to control stages
# additional constaints for stages
# there can be global varaibles that can be accessed all over flow und ones in a specific lexical scope def (stage_nr, lex_scope_within_scope)
# every stage should be a container (could be a class) that can call a functions